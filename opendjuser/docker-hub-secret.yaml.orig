kubectl create secret docker-registry myregistrykey \
  --docker-server=https://index.docker.io/v1/ \
  --docker-username=tnstamp \
  --docker-password=t3r3nc3s \
  --docker-email=terence.stamp@capgemini.com
